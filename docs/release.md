# Release

Signal Repeat uses version `0.0.0` during initial development. The first public
MVP release will be version `0.1.0`. Keep `package.json` and
`public/manifest.json` versions aligned for releases.

A release candidate must build successfully and satisfy the acceptance criteria
in `Signal-Repeat-SPECIFICATION.md`.
