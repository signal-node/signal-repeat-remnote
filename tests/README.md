# Tests

Unit and integration tests will be added with their corresponding behavior.
The initial test targets are target resolution, elapsed-time progress, session
state transitions, and settings validation.
